name: Deploy Jekyll with GitHub Pages + PageCrypt

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      site-path: ./_site
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Ruby / Bundler setup (build using your Gemfile so you can choose Jekyll version)
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.1 # change if you need a different Ruby version

      - name: Cache Ruby gems
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: ${{ runner.os }}-gems-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-gems-

      - name: Install gems
        run: |
          gem install bundler
          bundle config set --local path 'vendor/bundle'
          bundle install --jobs 4 --retry 3

      # Build the site with Jekyll (bundle exec ensures it uses your Gemfile's jekyll)
      - name: Build site with Jekyll
        run: bundle exec jekyll build --destination ./_site

      # --- Encryption: PageCrypt ---
      - name: Set up Node for PageCrypt
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Encrypt pages with PageCrypt
        # Expects _protected_pages.txt with lines like: path/to/page.html password123
        run: |
          if [ -f _protected_pages.txt ]; then
            set -o pipefail
            # Read each line; first token = file (relative to repo root), second token = password
            while IFS= read -r file password || [ -n "$file" ]; do
              # skip blank lines and comment lines
              [ -z "$file" ] && continue
              case "$file" in \#*) continue ;; esac
              echo "Encrypting _site/$file"
              npx --yes pagecrypt "_site/$file" "_site/$file" "$password"
            done < _protected_pages.txt
          else
            echo "_protected_pages.txt not found — skipping encryption step"
          fi

      # Upload built site as artifact for pages deployment
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./_site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
